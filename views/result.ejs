<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Result</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
        }
        .score {
            font-size: 1.5em;
            margin-top: 20px;
        }
        .progress-section {
            margin-top: 20px;
        }
        .progress-label {
            margin-bottom: 5px;
            font-size: 1.1em;
        }
        .user-info {
            margin-bottom: 10px;
            font-size: 1.2em;
        }
        .progress-bar {
            width: 100%;
            background-color: #e0e0e0;
            border-radius: 5px;
            overflow: hidden;
            height: 20px;
        }
        .progress {
            height: 100%;
            background-color: #4caf50;
            text-align: center;
            color: #fff;
            line-height: 20px;
            font-size: 0.9em;
            width: <%= p %>%;
        }
        a {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Quiz Completed!</h1>
        <div class="score">
            <p>Your score: <%= score %> out of <%= total %></p>
        </div>
        <div class="progress-section">
            <div class="user-info">
                <p>Username: <%= email %></p>
                <p>Course: <%= coursename %></p>
            </div>
            <div class="progress-label">Your Progress:</div>
            <div class="progress-bar">
                <div class="progress">
                    <%= p %>%
                </div>
            </div>
        </div>
        <a href="/course/:coursename/Quiz/<%=email%>">Take Quiz Again</a>
    </div>
</body>
</html>
