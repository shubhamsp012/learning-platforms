<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz App</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            background: #f4f4f9;
            color: #2c3e50;
        }

        h1, h2, h3 {
            font-weight: bold;
        }

        .container {
            max-width: 800px;
            margin: 30px auto;
            padding: 2rem;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .question {
            margin-bottom: 20px;
            font-size: 1.2rem;
        }

        .options {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            margin-top: 10px;
        }

        .options p {
            margin: 8px 0;
            font-size: 1rem;
            cursor: pointer;
        }

        input[type="radio"] {
            margin-right: 10px;
            accent-color: #74ebd5;
        }

        .submit-btn {
            padding: 0.8rem 2rem;
            font-size: 1.1rem;
            background-color: #2c3e50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 20px;
        }

        .submit-btn:hover {
            background-color: #74ebd5;
            color: #2c3e50;
        }

        footer {
            text-align: center;
            margin-top: 40px;
            font-size: 1rem;
        }

        footer p {
            color: #aaa;
        }

        footer a {
            text-decoration: none;
            color: #2c3e50;
            font-weight: bold;
        }

        footer a:hover {
            color: #74ebd5;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>Quiz</h1>
    <form action="/course/C%20Programming/result/<%=email%>" method="POST">
        <% for (let i = 0; i < data.length; i++) { %>
            <div class="question">
                <p><%= data[i].question %></p>
                <div class="options">
                    <% for (let j = 0; j < data[i].options.length; j++) { %>
                        <p>
                            <input type="radio" name="answer-<%= i %>" value="<%= data[i].options[j] %>">
                            <%= data[i].options[j] %>
                        </p>
                    <% } %>
                </div>
            </div>
        <% } %>
        <button class="submit-btn" type="submit">Submit</button>
    </form>
</div>

<footer>
    <p>Thank you for participating in the quiz! <a href="/">Take the quiz again</a></p>
</footer>

</body>
</html>
